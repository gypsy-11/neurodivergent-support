{
  "name": "neurodivergent-support-coaching",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "build": "npm run optimize-images && astro build",
    "build:analyze": "npm run optimize-images && astro build --verbose",
    "preview": "astro preview",
    "astro": "astro",
    "optimize-images": "node scripts/optimize-images.js",
    "lighthouse": "npx lighthouse http://localhost:4321 --output=html --output-path=./lighthouse-report.html",
    "performance-check": "npm run build && npm run lighthouse"
  },
  "dependencies": {
    "astro": "^5.13.2"
  },
  "devDependencies": {
    "terser": "^5.43.1",
    "lighthouse": "^11.6.0"
  }
}
